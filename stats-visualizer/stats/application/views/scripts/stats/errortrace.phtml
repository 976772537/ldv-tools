<?php
$this->form->setAction($this->url());
echo $this->form;
if(isset($this->entries['stats']))
{
//  foreach ($this->entries['stats'] as $entry)
//  {
    echo "<pre>", $this->escape($this->entries['stats']), "</pre>";
 // }
}
?>
